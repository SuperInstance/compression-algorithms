[package]
name = "compression-algorithms"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Compression algorithms from scratch in Rust — Huffman, arithmetic coding, BWT, LZ77"
repository = "https://github.com/SuperInstance/compression-algorithms"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
